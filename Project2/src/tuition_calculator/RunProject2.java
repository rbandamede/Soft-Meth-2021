package tuition_calculator;

/**
 * RunProject2 is the driver to run Project2.
 * It calls the run method in the TuitionManager class.
 @author Aileen Wu, Rishika Bandamede
*/
public class RunProject2 {
	/**
	 * Invokes the run() method in TuitionManager and runs Project2.
	*/
	public static void main(String[] args) {
		new TuitionManager().run();
	}
}
